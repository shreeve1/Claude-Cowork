# Install

Copy and paste this into your AI agent:

> Install the skills and agents from this repo into my Cowork workspace. Copy each folder from `.claude/skills/` into my `.claude/skills/` directory and each agent from `agents-library/` into my `.claude/agents/` directory, creating any missing folders. After that, for every skill folder, look inside for a `cowork/SKILL.md` file and output its contents as a `.SKILL` file — I'll click save in the desktop app to register the pointer skill.
