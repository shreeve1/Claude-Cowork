# Tracked policy: only cowork/ and CLAUDE.md are tracked.
# Everything else at root is local-only. Negate to add a new tracked root item.

/*
!/cowork/
!/CLAUDE.md
!/README.md
!/.gitignore

# macOS
.DS_Store
**/.DS_Store
